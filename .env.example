# Provider credentials
GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# Gateway authentication. Generate with:
# python -c "import secrets; print('sk-' + secrets.token_urlsafe(32))"
LITELLM_MASTER_KEY=

# Local listener settings used by scripts/start_proxy.py
HOST=127.0.0.1
PORT=4000
